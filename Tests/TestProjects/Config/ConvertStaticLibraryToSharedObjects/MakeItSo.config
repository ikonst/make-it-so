<MakeItSo>
  
  <!-- Settings that apply to all projects in the solution -->
  <AllProjects>
  </AllProjects>

  <!-- Settings for the 'Hello' project -->
  <Project name="Hello">

    <!-- We build the Hello library as shared-objects, even though it is a static library in Visual Studio -->
    <ConvertStaticLibraryToSharedObjects convert="true"/>

  </Project>
  
</MakeItSo>