<MakeItSo>
  
  <!-- Settings that apply to all projects in the solution -->
  <AllProjects>

    <!-- We replace the -O2 optimization flag with -O3 -->
    <RemoveCompilerFlag flag="-O2"/>
    <AddCompilerFlag configuration="Release" flag="-O3"/>
    
  </AllProjects>
  
</MakeItSo>